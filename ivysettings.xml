<ivysettings>
  <settings defaultResolver="chain"/>
  <resolvers>
    <chain name="chain">
      <filesystem name="project">
	    <artifact pattern="${ivy.settings.dir}\lib\[organisation]\[artifact]-[revision].[ext]" />
      </filesystem>
      <ibiblio name="ibiblio" m2compatible="true" />
      <ibiblio name="instinct" root="http://www.easyb.org/repo/" m2compatible="true"/>
    </chain>
  </resolvers>
</ivysettings>